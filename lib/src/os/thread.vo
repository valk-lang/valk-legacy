
header "pthread" as pt;

use volt:mem;

class Thread {

    - static fn entry(data: ptr) void {
		// let func = data @as fn()(void);
		// func();
	}

	static fn start(start_func: ptr) void {

    	let thr = mem:alloc(24 @as uint) @as pt:pthread_t;
    	let err = pt:pthread_create(thr, 0 @as ptr, start_func, 0 @as ptr);
    	// if err != 0 {
		// 	throw fail;
    	// }
		// let t = Thread{ os_thread: thr };
	}
}
