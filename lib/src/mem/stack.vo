
use volt:mem

struct Stack {
    data : ptr
    count : int (0)

    static fn new() Stack {
        let s = 1
        let x = Stack {
            data: mem:alloc(10000 @as uint)
        }
        return x;
    }
}
